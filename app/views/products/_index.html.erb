<div id="notice">Hey there! <%=flash[:notice] %></div>


<!-- **The following div is temporary! ** List top categories here
   * put top_categories array in application controller (right now only in catoegory controller-also need in welcome controller) -->

<!-- <div class="sidebar">
  <#% Category.all.each do |category| %>
  <#%= link_to "#{category.name.upcase}", category_path(category) %>
  <#% end %>
</div> -->

<div class="productlist">
  <% if session[:user_id] %>
    <% @products.each do |product| %>
      <% if product.user_id != current_user.id %>
      <ul class="individualproduct">
        <% unless product[:status] == "retired" %>
        <li id="productprice"><li><%= link_to "#{product.name.capitalize}", product_path(product.id) %></li>
        <li id="PICTURE">PICTURE</li>
        <li><%= number_to_currency(product.price) %></li>
        <li id="cartbutton">
            <%= form_tag(add_product_path(current_order.id, product.id), method: "post") do %>
                  <%=  number_field_tag(:quantity, 1, in: 1..product.inventory) %>
                  <%=  submit_tag "Add to Cart" %>
                <% end %>
            <% end %>
          <% end %>
        </li>
      </ul>
    <% end %>
  <%end %>      
      
</div>

  


