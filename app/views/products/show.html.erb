<div class= "productshow">
  <h1><%= @product.name.capitalize %></h2>
  <ul>
    <li>$<%= @product.price %></li> <!-- this isn't showing the two zeros! -->
    <li><%= @product.inventory%></li>
    <li><%= @product.description %></li>
    <li><%= form_tag(add_product_path(current_order.id, @product.id), method: "post") do %>
            <%=  number_field_tag(:quantity) %>
            <%=  submit_tag "Add to Cart" %>
          <% end %></li>
    <li>Categories:<% unless @product.categories == [] %>
      <% @product.categories.each do |category|%>
          <%= link_to "#{category.name}", category_path(category.id) %>
        <% end %>
      <% end %>
    </li>      
  </ul>
</div>
<%= link_to "Back to browsing", products_path %>

<% if current_user %>
  <% if @product.user_id == current_user.id %>
    <%= button_to "Edit this product", edit_product_path(@product.id), method: :get %>
  <% end %>
<% end %>
